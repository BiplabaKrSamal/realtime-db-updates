[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = server
addopts =
    -v
    --tb=short
    -p no:warnings
markers =
    integration: requires a live PostgreSQL instance (deselect with -m "not integration")
